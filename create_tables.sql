create table links (link text,
                    page_title text,
                    message text,
                    user text,
                    timestamp datetime default current_timestamp
                    );
